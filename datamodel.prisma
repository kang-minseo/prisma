type User {
  id: ID! @id
  name: String!
  email: String!
}

type Post {
  id: ID! @id
  title : String!
  content: String!
  comment: String!
  views: Int!
}

type comment {
  id : ID! @id
  comment: String
}